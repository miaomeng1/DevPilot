# 本地真实接入验收 · 2026-09-05

本次使用真实 GitHub Actions、私有 GHCR 和本地隔离 Dokploy。DevPilot 后端使用临时 H2 数据库，真实客户端（非 UI mock）；不代表 MySQL/Redis 生产部署验收。

## 已验证

- 私有示例仓库：`miaomeng1/devpilot-e2e-demo`。
- 自动检测根目录 Dockerfile 和 Node 测试入口。
- 自动创建专用 Dokploy 项目、production 环境、应用和 `18088 → 8080` 映射，配置私有 Registry 拉取凭据。
- 自动写入独立 GitHub Environment Secrets，生成 [PR #1](https://github.com/miaomeng1/devpilot-e2e-demo/pull/1)。
- 合并触发 [push run 33936571494](https://github.com/miaomeng1/devpilot-e2e-demo/actions/runs/33936571494)，测试、安全扫描、amd64/arm64 镜像构建推送成功，production 跳过。
- GHCR API 确认包 visibility 为 private。
- 用户手动触发 [run 33936747095](https://github.com/miaomeng1/devpilot-e2e-demo/actions/runs/33936747095)，签名回调成功，Dokploy 实际拉取私有镜像。
- attempt 1 因 Agent 不识别 JSON `status: ok` 被标记 UNHEALTHY；修复 Agent 并通过测试后，仅重试该已确认版本的 production job。
- attempt 2 的 DevPilot deployment `2096051055735943169` 最终 HEALTHY；Provider deployment `YsmTw64RMbGqrvmLYCL_I`。
- 版本 `9e82b7b92a430f42ded3f4846b9407d974c3ae46`；业务返回 HTTP 200，`{"status":"ok","version":"v1"}`。
- 容器从 `55549925f1d7` 替换为 `6615e793935c`，DevPilot 自动重关联并核对目标镜像，新鲜健康探测通过。
- 临时回调网关只允许该应用 POST 回调：管理路由 404，无签名回调 401。

## 发现与修复

- Dokploy v0.30.3 创建项目返回 `{project, environment}`，已兼容包装结构并新增回归测试。旧代码的重试成功复用原项目，没有重复创建。
- Dokploy Key 默认每天 10 次请求，耗尽返回 401。测试 Key 改为每分钟 1000 次、24 小时有效，原 Key 已撤销；使用说明补充配额提示。
- Agent 健康状态兼容大小写及空白的 `OK`，保留 DOWN/degraded/unknown 拒绝行为，Agent 全量测试通过。

## 未验证 / 限制

- v2 更新和故障版本自动回滚已验证（见下）；人工回滚目前只有集成测试，不冒充真实故障恢复验收。
- 尚未验证真实 GitLab、Coolify、云服务器网络、DNS/TLS 和持久化生产安装。
- 本次安全门禁是源代码/依赖/Secret/IaC 扫描，不是发布镜像全层扫描。
- v1 旧模板手动 workflow run 会重建同 SHA 的镜像 tag，因此其 push 与手动 run 摘要不同。v2 新模板已消除此问题；既有旧 workflow 需要显式更新。
- 私有仓库、测试镜像及本地容器保留；临时隧道关闭后不能继续接收 GitHub 回调，后续测试需要恢复回调地址。

## v2 原始 digest 发布验证

- 构建 [33937258763](https://github.com/miaomeng1/devpilot-e2e-demo/actions/runs/33937258763) 成功，提交 `f5ea481c418bf4af41072cc5e3b23a60adf78dac`。
- 从该构建下载的 `devpilot-release-e2e-demo` 凭证记录摘要 `sha256:364ff547b1acd6edb107ade413e622e908eba7c4f2c7aa0d2d6c366db6d52895`。
- 用户确认的发布 [33938047034](https://github.com/miaomeng1/devpilot-e2e-demo/actions/runs/33938047034) 中仅 production 运行成功；quality/security/image 均跳过，未重新构建。
- DevPilot deployment `2096056978500038658` 和 Dokploy deployment `x1KIdlxhNbBu0KsxHQm38` 完成，HEALTHY；部署 URI 与上述构建摘要完全一致。
- 容器从 `6615e793935c` 替换为 `380bdddea5ee`，Agent 自动关联新容器。目标服务器健康端点返回 `{"status":"ok","version":"v2"}`。
- Agent 同时报告内部 `8080/tcp` 与服务发布映射 `Swarm ingress :18088→8080/tcp`。

## 故障版本与自动回滚验证

- 测试镜像 v3-fault 通过 Dockerfile 显式注入 `DEVPILOT_E2E_FAIL_HEALTH=1`，健康端点返回 503/DOWN。构建与单元测试可成功，这是运行时故障注入，不是假装测试失败。
- 构建 [33941157595](https://github.com/miaomeng1/devpilot-e2e-demo/actions/runs/33941157595)；用户确认发布 [33941522924](https://github.com/miaomeng1/devpilot-e2e-demo/actions/runs/33941522924)。
- 故障镜像摘要 `sha256:a66062b0332a4cbd60ae9c50253d0b58901a012cfaaa51408796c2143060ca0f`，发布记录 `2096075700501557249`。
- Swarm task `0xi2n507q39otjrmal08gu33d` 失败原因为 `dockerexec: unhealthy container`。DevPilot 在截止时间未确认到持续运行的目标容器，将发布判失败，没有因 CI 成功或旧版本健康而误判上线。
- 测试应用既有配置启用了自动回滚，创建独立回滚 `2096077000479944706`，关联失败发布 ID，恢复 v2 摘要 `sha256:364ff547b1acd6edb107ade413e622e908eba7c4f2c7aa0d2d6c366db6d52895`。
- Dokploy 回滚任务 `g7ubJ89kc8jVHhRtgRuBv` 完成；新容器 `417c322a8f1a` 自动关联，Agent 新鲜健康探测通过。直接请求目标机端口确认 `{"status":"ok","version":"v2"}`。
- 此次运行暴露了旧后端原失败记录停留在 ROLLBACK_TRIGGERED 的展示问题。源码已增加 ROLLED_BACK / ROLLBACK_FAILED 终态，并避免回滚结果改写原健康版本的流水线历史；运行中的 H2 实例尚未重启，不伪造修复后的历史状态。

## 收尾验证与凭据清理

### 后续预检增量（尚未完成新版实例验收）

- `ea75c96` 已加入 GitHub REST core 配额、已知无 push 权限、npm 测试入口与锁文件，以及目标机 TCP 监听端口和 Docker 发布映射检查。
- Linux 隔离环境执行新增 Agent 测试：创建真实随机 TCP listener，采集正确发现该端口；后端验证端口心跳入库、过期数据拒绝和已占用端口拒绝。上述测试不替代新版实例完整接入验收。
- 检查当前 Dokploy 容器构建代码：`/api-key/list` 使用登录会话中间件，Key 模型包含 `rateLimitMax`、`rateLimitTimeWindow`、`requestCount` 等字段；`user.getPermissions` 存在，但尚未用有效 API Key 验证其响应和访问能力。不能据此断言普通 API Key 已具备全部权限或足够配额。
- 原临时部署 Key 已撤销。完整权限、Dokploy 配额自动预检及新版实例端到端测试仍待完成，不以兼容性调查作为通过证据。
- 用户重新授权后创建了新的 24 小时临时 Key：实测 `user.getPermissions` 和 `project.all` 返回 200；`/api/auth/api-key/list` 返回 401，项目响应未提供配额头。源码已接入必要权限逐项检查；新 Key 暂用于后续验收，不在文档或代码中记录其值，验收后仍需撤销。

### 新版实例真实端口预检（07464be）

- 独立新版后端监听本机 18090，使用临时 H2；原演示后端和 v2 应用未重启。新版 Linux arm64 Agent 通过仅允许 Agent 路由的 18191 网关连接，服务器 ID `2096102862366994434`。
- 在 Linux 目标机用临时进程监听 TCP 18089。接入任务 `87cc2952-bdeb-4365-99f2-8621d78175f6` 停留 stage 0 / FAILED，明确返回“目标服务器 TCP 端口 18089 已被进程监听”。没有 resourceId。
- 同一目标机空闲 TCP 18090 的任务 `5f6a0619-0f5c-471e-8f1a-ea8d2729e6e0` 进入 stage 1 / PENDING，未执行 stage 1。注意目标 Linux 与后端 Mac 的网络命名空间不同，此处检查的是目标 Linux 端口。
- 预检调用真实 GitHub 仓库/配额、Dokploy 权限/项目和真实 Agent 心跳。前后 Dokploy projectId 集合一致，没有创建部署资源、PR、Secrets 或触发发布。测试监听进程已停止。
- 这验证了新版实例预检链路，不代替新版完整发布/回滚验收；Dokploy 剩余配额仍不能由该 API Key 自动读取。

### 首次收尾基线（14b2ed8）

- 当时的 `make test` 通过：后端 56 项测试、前端 9 项模板测试、类型检查和生产构建、Agent 全量测试。新增回归覆盖部署平台直接拒绝回滚时正确结束原失败记录，且不改写历史健康流水线、不再次回滚。
- `scripts/verify-cicd.sh` 和 `git diff --check` 通过；对 462 个非忽略文件进行本次已知测试凭据精确匹配检查，无命中。这不替代完整安全审计。
- 已关闭临时公网回调隧道，删除示例仓库 `production-e2e-demo` 环境下的两项 DevPilot 回调 Secrets。
- 已撤销临时 Dokploy Key，后续认证读取返回 401；清空本次专用应用的 Registry 用户名和密码，并移除 Dokploy Docker 客户端中匹配本次 Token 的 GHCR 登录缓存。没有撤销用户自身 GitHub 登录。
- 私有仓库、镜像、现有项目及运行中的 v2 容器保留；没有删除业务数据。再次发布必须重新配置拉取凭据、有效部署 Key 和回调地址/Secret。运行中本地测试实例保留原加密配置，已撤销 Key 不再有效。
- 自动接入目前只能提前检查已暴露的仓库权限、平台连通性与 Dokploy 应用端口占用；没有完整自动核验所有写权限、Key 剩余额度及平台外进程端口。权限与配额仍需按接入指南确认，不能宣称任意仓库零配置接入。
